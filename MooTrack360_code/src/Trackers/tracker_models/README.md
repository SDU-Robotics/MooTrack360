The following folder, tracker_models, contains the source code from each tracker.

Note that they have been added as submodule, meaning cloning VelKoTek down on a new PC, you have to init your submodules:

git submodule update --init --recursive


NOTE: Changed from using submodule to simply manually copy over the parts needed, meaning the above is no longer in use
